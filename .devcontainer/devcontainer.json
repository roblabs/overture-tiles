// For format details, see https://aka.ms/devcontainer.json. For config options, see the
{
	"name": "OvertureMaps.org",
	// Or use a Dockerfile or Docker Compose file. More info: https://containers.dev/guide/dockerfile
	// https://github.com/devcontainers/images/blob/main/src/base-ubuntu/README.md
	"image": "mcr.microsoft.com/devcontainers/base:ubuntu",

	// Features to add to the dev container. More info: https://containers.dev/features.
	"features": {
		"ghcr.io/devcontainers/features/aws-cli": {},
		"ghcr.io/devcontainers/features/common-utils:2": {
			"installZsh": "true",
			"username": "overture",
			"upgradePackages": "true"
		},
		"ghcr.io/devcontainers/features/docker-in-docker:3": {
			"version": "latest",
			"moby": false
		}
	},

	// Mount:  Docker & AWS
	"mounts": [
        // uncomment To use AWS CLI.  The folder ~/.aws must exist.
        "source=${localEnv:HOME}/.aws,target=/home/overture/.aws,type=bind,consistency=cached",

        // uncomment To use Azure CLI.  The folder ~/.azure must exist.
        // "source=${localEnv:HOME}/.azure,target=/home/node/.azure,type=bind,consistency=cached",

        // uncomment To use your global Git config & ignore.  The files ~/.gitconfig and ~/.gitignore must exist.
        "source=${localEnv:HOME}/.gitconfig,target=/home/overture/.gitconfig,type=bind,consistency=cached",
        "source=${localEnv:HOME}/.gitignore,target=/home/overture/.gitignore,type=bind,consistency=cached",

		"source=/var/run/docker.sock,target=/var/run/docker-host.sock,type=bind"
	],

	// Configure tool-specific properties.
	"customizations": {
		// Configure properties specific to VS Code.
		"vscode": {
			"settings": {},
			"extensions": [
				"amazonwebservices.aws-toolkit-vscode",
				"CTC.vscode-tree-extension",
				"ms-azuretools.vscode-docker",
				"ms-vscode-remote.remote-containers",
				"swallow.geojson-io-for-vscode",
				"skellock.just"
			]
		}
	},

	// Use 'postCreateCommand' to run commands after the container is created.
	"postCreateCommand": "/bin/sh ./.devcontainer/postCreateCommand.sh",

	// Set `remoteUser` to `root` to connect as root instead. More info: https://aka.ms/vscode-remote/containers/non-root.
	"remoteUser": "overture"
}
